# Flask Backend

Standard Flask application structure using application factory pattern, Blueprints, SQLAlchemy, and JWT.

- Entry points: `manage.py`, `wsgi.py`
- App package: `app/`
- Migrations: `migrations/`
- Tests: `tests/`
- Dependencies: `requirements/`
